export type TextScrollerProps = {
  texts: string[];
  className: string;
  scrollProgress: number;
  multiplier: number;
};

export type SkillsLineProps = {
  items: string[];
  scrollProgress: number;
  multiplier: number;
};